# UX 打磨：深色模式对比度 + 列表动画 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 深色模式下 AI Chat 气泡边框对比度提升 + NoteLanding 笔记列表删除/完成时 slide-out 动画反馈。

**Architecture:** Part 1 仅修改两个 alpha 数值。Part 2 在 `_NoteLandingState` 中新增退出动画状态管理（`_removingIds` + `_removeAnimations`），在删除/标记完成时播放 SlideTransition + FadeTransition，动画结束后执行实际操作。

**Tech Stack:** Flutter AnimationController / SlideTransition / FadeTransition / TickerProviderStateMixin

---

## File Map

| 文件 | 操作 | 职责 |
|------|------|------|
| `lib/screen/AiChat.dart` | 修改 | AI 气泡边框 alpha 0.15 → 0.3，工具气泡边框 alpha 0.15 → 0.3 |
| `lib/screen/NoteLanding.dart` | 修改 | 添加 slide-out 动画（TickerProviderStateMixin + 动画控制 + 卡片包裹） |

---

## Task 1: 深色模式对比度微调

**Files:**
- Modify: `lib/screen/AiChat.dart:1502` (AI bubble border)
- Modify: `lib/screen/AiChat.dart:1661` (tool bubble border)

- [ ] **Step 1: 修改 AI 气泡边框 alpha**

在 `_buildMessageBubble` 中找到 line 1502：

```dart
color: colorScheme.primary.withValues(alpha: 0.15),
```

替换为：

```dart
color: colorScheme.primary.withValues(alpha: 0.3),
```

- [ ] **Step 2: 修改工具气泡边框 alpha**

在 `_buildToolBubble` 中找到 line 1661：

```dart
color: colorScheme.outline.withValues(alpha: 0.15),
```

替换为：

```dart
color: colorScheme.outline.withValues(alpha: 0.3),
```

- [ ] **Step 3: 验证**

```bash
flutter analyze lib/screen/AiChat.dart
```

Expected: No error-level issues

- [ ] **Step 4: Commit**

```bash
git add lib/screen/AiChat.dart
git commit -m "fix: improve dark mode contrast for AI chat bubble borders"
```

---

## Task 2: 笔记列表 slide-out 动画

**Files:**
- Modify: `lib/screen/NoteLanding.dart:34` (class mixin)
- Modify: `lib/screen/NoteLanding.dart:38-54` (state variables)
- Modify: `lib/screen/NoteLanding.dart:382-393` (dispose)
- Modify: `lib/screen/NoteLanding.dart:1153` (card wrapper)
- Modify: `lib/screen/NoteLanding.dart:764-769` (delete logic)
- Modify: `lib/screen/NoteLanding.dart:1184-1187` (checkbox onChanged)

- [ ] **Step 1: 添加 TickerProviderStateMixin**

找到 line 34：

```dart
class _NoteLandingState extends State<NoteLanding> {
```

替换为：

```dart
class _NoteLandingState extends State<NoteLanding>
    with TickerProviderStateMixin {
```

- [ ] **Step 2: 添加动画状态变量**

在 line 54（`bool _isTranscribing = false;` 之后）添加：

```dart
  final Set<int> _removingIds = {};
  final Map<int, AnimationController> _removeAnimations = {};
```

- [ ] **Step 3: 添加 `_animateRemoval` 方法**

在 `_toggleCardExpansion` 方法之前（line 56 附近）添加：

```dart
  void _animateRemoval(int id, VoidCallback onComplete) {
    if (_removingIds.contains(id)) return;
    final controller = AnimationController(
      duration: const Duration(milliseconds: 300),
      vsync: this,
    );
    _removeAnimations[id] = controller;
    setState(() => _removingIds.add(id));
    controller.forward().then((_) {
      _removingIds.remove(id);
      _removeAnimations.remove(id);
      controller.dispose();
      onComplete();
    });
  }
```

- [ ] **Step 4: 修改 dispose 清理动画**

找到 `dispose()` 方法（line 382-393），在 `super.dispose();` 之前添加清理：

```dart
  @override
  void dispose() {
    for (final sub in _aiSubs) {
      sub.cancel();
    }
    _recordingTimer?.cancel();
    _recorder.dispose();
    _ctrls.forEach((key, value) {
      value.dispose();
    });
    for (final controller in _removeAnimations.values) {
      controller.dispose();
    }
    super.dispose();
  }
```

- [ ] **Step 5: 修改删除逻辑，动画结束后再删除**

找到 `_confirmDelete` 中的确认按钮 onPressed（line 765-769）：

```dart
                      onPressed: () {
                        db.deleteNoteItem(item);
                        Navigator.of(context).pop();
                        _updateUI(context);
                      })
```

替换为：

```dart
                      onPressed: () {
                        Navigator.of(context).pop();
                        _animateRemoval(item.id!, () {
                          db.deleteNoteItem(item);
                          _updateUI(context);
                        });
                      })
```

- [ ] **Step 6: 修改 checkbox onChanged，hideDone 模式下触发动画**

找到 checkbox `onChanged`（line 1184-1187）：

```dart
                    onChanged: (bool? newValue) {
                      setState(() => item.isDone = newValue ?? false);
                      db.toggleNoteItem(item);
                    },
```

替换为：

```dart
                    onChanged: (bool? newValue) {
                      setState(() => item.isDone = newValue ?? false);
                      db.toggleNoteItem(item);
                      final switcherProvider =
                          Provider.of<SwitcherChangeNotifier>(context,
                              listen: false);
                      if (switcherProvider.isHiddenDone() &&
                          newValue == true) {
                        _animateRemoval(item.id!, () {
                          _updateUI(context);
                        });
                      }
                    },
```

- [ ] **Step 7: 在卡片渲染处包裹动画**

找到 `_buildItemList` 中 `return Padding(` 处（line 1153），将整个 Padding widget 包裹动画。

在 `return Padding(` 之前插入动画包裹逻辑，最终结构：

```dart
      Widget card = Padding(
        key: Key('${item.id}'),
        padding: const EdgeInsets.symmetric(vertical: 0),
        child: Card(
          // ... existing card code unchanged ...
        ),
      );

      if (_removingIds.contains(item.id)) {
        final controller = _removeAnimations[item.id!]!;
        final slideAnimation = Tween<Offset>(
          begin: Offset.zero,
          end: const Offset(-1.0, 0.0),
        ).animate(CurvedAnimation(parent: controller, curve: Curves.easeIn));
        final fadeAnimation = Tween<double>(
          begin: 1.0,
          end: 0.0,
        ).animate(CurvedAnimation(parent: controller, curve: Curves.easeIn));
        card = SlideTransition(
          key: Key('${item.id}'),
          position: slideAnimation,
          child: FadeTransition(opacity: fadeAnimation, child: card),
        );
      }

      return card;
```

注意：将原来的 `return Padding(...)` 改为 `Widget card = Padding(...)`（去掉 key 从 Padding 移到外层逻辑），最终 `return card;`。Key 在动画包裹时移到 SlideTransition 上，非动画时保留在 Padding 上。

- [ ] **Step 8: 验证**

```bash
flutter analyze lib/screen/NoteLanding.dart
```

Expected: No error-level issues

- [ ] **Step 9: Commit**

```bash
git add lib/screen/NoteLanding.dart
git commit -m "feat: add slide-out animation for note deletion and completion"
```

---

## Task 3: 集成验证

- [ ] **Step 1: 全量静态分析**

```bash
flutter analyze lib/
```

Expected: No error-level issues

- [ ] **Step 2: 构建 APK**

```bash
flutter build apk --debug --target-platform android-arm64
```

Expected: Build successful

- [ ] **Step 3: 手动测试 Checklist**

Part 1:
1. 深色模式下 AI Chat → AI 气泡边框清晰可见
2. 深色模式下工具气泡边框清晰可见
3. 浅色模式下边框不过于突兀

Part 2:
1. 删除已完成笔记 → 确认后卡片向左滑出消失 → 列表刷新
2. hideDone 模式下勾选完成 → 卡片向左滑出 → 列表刷新
3. 非 hideDone 模式下勾选完成 → 无动画，卡片原地状态变化
4. 拖拽排序仍正常工作
5. 快速连续删除多个笔记 → 各自独立动画，不冲突
